docker run -p 3030:3030 -it centos:latest /bin/bash
yum install git
git clone https://github.com/osuosl/fenestra.git /opt/code
cd /opt/code
git checkout issues/31/timesync_widget

# JS stuff
curl --silent --location https://rpm.nodesource.com/setup_4.x | bash -
yum install -y nodejs
npm install -g bower
bower install --allow-root

# Ruby Stuff
yum install make ImageMagick gcc-c++ ImageMagick-devel which vim

# RVM
gpg --keyserver hkp://keys.gnupg.net --recv-keys
409B6B1796C275462A1703113804BB82D39DC0E3
\curl -sSL https://get.rvm.io | bash -s stable
source /etc/profile.d/rvm.sh
rvm install ruby-2.2.2
rvm default use 2.2.2
gem install bundler
bundle install
#Copy config.yml.sample to config.yml
#Remove the comment at the top
cd jobs
git clone https://github.com/osuosl/rimesync.git
cd rimesync
git checkout develop
